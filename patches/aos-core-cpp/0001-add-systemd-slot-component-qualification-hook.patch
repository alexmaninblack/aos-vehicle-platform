SPDX-FileCopyrightText: 2026 maninblack
SPDX-License-Identifier: Apache-2.0

diff --git a/src/cm/smcontroller/tests/smcontroller.cpp b/src/cm/smcontroller/tests/smcontroller.cpp
index 9984a1c..5216751 100644
--- a/src/cm/smcontroller/tests/smcontroller.cpp
+++ b/src/cm/smcontroller/tests/smcontroller.cpp
@@ -111,8 +111,11 @@ protected:
 
 TEST_F(SMControllerTest, SMClientConnected)
 {
+    constexpr auto cComponentType = "aos-vm-1.0.0-main-qemuarm64-vehicle-data-provider";
+
     // 1) Start client
     SMClientStub client;
+    client.SetRuntimeType(cComponentType);
 
     auto err = client.Init(cMainNodeID);
     ASSERT_TRUE(err.IsNone()) << err.Message();
@@ -135,6 +138,9 @@ TEST_F(SMControllerTest, SMClientConnected)
     auto smInfo = mSMInfoReceiver.GetSMInfo(cMainNodeID);
 
     EXPECT_EQ(smInfo.mNodeID, String(cMainNodeID));
+    ASSERT_EQ(smInfo.mRuntimes.Size(), 1U);
+    EXPECT_EQ(smInfo.mRuntimes[0].mRuntimeType, String(cComponentType));
+    EXPECT_EQ(smInfo.mRuntimes[0].mArchInfo.mArchitecture, String("arm64"));
 
     // 4) Stop client
     err = client.Stop();
diff --git a/src/cm/smcontroller/tests/stubs/smclientstub.hpp b/src/cm/smcontroller/tests/stubs/smclientstub.hpp
index f253df7..c5d6676 100644
--- a/src/cm/smcontroller/tests/stubs/smclientstub.hpp
+++ b/src/cm/smcontroller/tests/stubs/smclientstub.hpp
@@ -45,6 +45,8 @@ public:
         return ErrorEnum::eNone;
     }
 
+    void SetRuntimeType(const std::string& runtimeType) { mRuntimeType = runtimeType; }
+
     Error Start(const std::string& url)
     {
         std::lock_guard lock {mMutex};
@@ -402,6 +404,15 @@ private:
         auto* smInfo = outMsg.mutable_sm_info();
         smInfo->set_node_id(mNodeID);
 
+        if (!mRuntimeType.empty()) {
+            auto* runtime = smInfo->add_runtimes();
+            runtime->set_runtime_id("r61-runtime-id");
+            runtime->set_type(mRuntimeType);
+            runtime->mutable_arch_info()->set_architecture("arm64");
+            runtime->mutable_os_info()->set_os("linux");
+            runtime->set_max_instances(1);
+        }
+
         if (!mStream) {
             return AOS_ERROR_WRAP(Error(ErrorEnum::eFailed, "stream not available"));
         }
@@ -620,6 +631,7 @@ private:
     }
 
     std::string                                               mNodeID;
+    std::string                                               mRuntimeType;
     std::atomic<bool>                                         mRunning;
     std::thread                                               mReadThread;
     std::thread                                               mNetworkUpdateThread;
diff --git a/src/sm/launcher/CMakeLists.txt b/src/sm/launcher/CMakeLists.txt
index e3aa20d..ee6a213 100644
--- a/src/sm/launcher/CMakeLists.txt
+++ b/src/sm/launcher/CMakeLists.txt
@@ -20,6 +20,10 @@ set(LIBRARIES Poco::JSON aos::core::common::tools aos::sm::runtimes::container a
               aos::sm::runtimes::rootfs aos::sm::utils
 )
 
+if(AOS_SYSTEMD_SLOT_COMPONENT_DIR)
+    list(APPEND LIBRARIES aos::sm::runtimes::systemdslotcomponent)
+endif()
+
 # ######################################################################################################################
 # Target
 # ######################################################################################################################
diff --git a/src/sm/launcher/runtimes.cpp b/src/sm/launcher/runtimes.cpp
index 4de938c..33c2812 100644
--- a/src/sm/launcher/runtimes.cpp
+++ b/src/sm/launcher/runtimes.cpp
@@ -10,6 +10,10 @@
 #include "runtimes/boot/boot.hpp"
 #include "runtimes/rootfs/rootfs.hpp"
 
+#ifdef AOS_SYSTEMD_SLOT_COMPONENT_RUNTIME
+#include <runtime.hpp>
+#endif
+
 namespace aos::sm::launcher {
 
 /***********************************************************************************************************************
@@ -59,6 +63,16 @@ Error Runtimes::Init(const Config& config, iamclient::CurrentNodeInfoProviderItf
             }
 
             mRuntimes.emplace_back(std::move(runtime));
+#ifdef AOS_SYSTEMD_SLOT_COMPONENT_RUNTIME
+        } else if (runtimeConfig.mPlugin == cRuntimeSystemdSlotComponent) {
+            auto runtime = std::make_unique<SystemdSlotComponentRuntime>();
+
+            if (auto err = runtime->Init(runtimeConfig, currentNodeInfoProvider); !err.IsNone()) {
+                return AOS_ERROR_WRAP(err);
+            }
+
+            mRuntimes.emplace_back(std::move(runtime));
+#endif
         } else {
             return AOS_ERROR_WRAP(Error(ErrorEnum::eNotSupported, "runtime is not supported"));
         }
diff --git a/src/sm/launcher/runtimes/CMakeLists.txt b/src/sm/launcher/runtimes/CMakeLists.txt
index 5bde7a1..05f984e 100644
--- a/src/sm/launcher/runtimes/CMakeLists.txt
+++ b/src/sm/launcher/runtimes/CMakeLists.txt
@@ -19,3 +19,13 @@ add_subdirectory(boot)
 add_subdirectory(container)
 add_subdirectory(rootfs)
 add_subdirectory(utils)
+
+if(AOS_SYSTEMD_SLOT_COMPONENT_DIR)
+    if(NOT IS_ABSOLUTE "${AOS_SYSTEMD_SLOT_COMPONENT_DIR}"
+       OR NOT EXISTS "${AOS_SYSTEMD_SLOT_COMPONENT_DIR}/CMakeLists.txt"
+    )
+        message(FATAL_ERROR "AOS_SYSTEMD_SLOT_COMPONENT_DIR must be an absolute runtime source directory")
+    endif()
+
+    add_subdirectory("${AOS_SYSTEMD_SLOT_COMPONENT_DIR}" "${CMAKE_CURRENT_BINARY_DIR}/systemd-slot-component")
+endif()
diff --git a/src/sm/launcher/tests/launcher.cpp b/src/sm/launcher/tests/launcher.cpp
index f2d6cbc..e41cb98 100644
--- a/src/sm/launcher/tests/launcher.cpp
+++ b/src/sm/launcher/tests/launcher.cpp
@@ -22,6 +22,10 @@
 #include <sm/launcher/runtimes/rootfs/rootfs.hpp>
 #include <sm/tests/mocks/systemdconnmock.hpp>
 
+#ifdef AOS_SYSTEMD_SLOT_COMPONENT_RUNTIME
+#include <runtime.hpp>
+#endif
+
 using namespace testing;
 
 namespace aos::sm::launcher {
@@ -107,6 +107,13 @@ TEST_F(RuntimesTest, InitRuntimes)
         RuntimeConfig {cRuntimeBoot, "aos-vm-boot", true, "", Poco::makeShared<Poco::JSON::Object>()});
     config.mRuntimes.emplace_back(
         RuntimeConfig {cRuntimeRootfs, "aos-vm-rootfs", true, "", Poco::makeShared<Poco::JSON::Object>()});
+#ifdef AOS_SYSTEMD_SLOT_COMPONENT_RUNTIME
+    auto componentConfig = Poco::makeShared<Poco::JSON::Object>();
+    componentConfig->set("qualificationMode", true);
+    config.mRuntimes.emplace_back(RuntimeConfig {cRuntimeSystemdSlotComponent,
+        "aos-vm-1.0.0-main-qemuarm64-vehicle-data-provider", true,
+        "/var/aos/workdirs/sm/runtimes/systemd-slot-component", componentConfig});
+#endif
     auto nodeInfo = std::make_unique<NodeInfo>();
 
     CreateNodeInfo(*nodeInfo);
diff --git a/src/sm/smclient/tests/smclient.cpp b/src/sm/smclient/tests/smclient.cpp
index 76dee89..59bb600 100644
--- a/src/sm/smclient/tests/smclient.cpp
+++ b/src/sm/smclient/tests/smclient.cpp
@@ -187,7 +187,7 @@ TEST_F(SMClientTest, SendSMInfoWithMultipleRuntimesAndResources)
     for (int i = 0; i < 3; i++) {
         RuntimeInfo runtime;
         runtime.mRuntimeID   = (std::string("runtime") + std::to_string(i)).c_str();
-        runtime.mRuntimeType = "runc";
+        runtime.mRuntimeType = i == 2 ? "aos-vm-1.0.0-main-qemuarm64-vehicle-data-provider" : "runc";
         runtimes->PushBack(runtime);
     }
 
@@ -226,6 +226,7 @@ TEST_F(SMClientTest, SendSMInfoWithMultipleRuntimesAndResources)
 
     EXPECT_CALL(*server, OnSMInfo(_)).WillOnce(Invoke([](const smproto::SMInfo& info) {
         EXPECT_EQ(info.runtimes_size(), 3);
+        EXPECT_EQ(info.runtimes(2).type(), "aos-vm-1.0.0-main-qemuarm64-vehicle-data-provider");
         EXPECT_EQ(info.resources_size(), 2);
     }));
 
